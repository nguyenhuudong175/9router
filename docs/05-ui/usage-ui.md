# Usage UI

## Location
- `src/app/(dashboard)/dashboard/usage/**`.

## Components
- Overview cards, usage chart, usage table, request details tab, provider topology, provider limit cards.

## Backend dependency
- Pulls from usage APIs (`/api/usage/stats`, `/chart`, `/history`, `/request-details`, `/stream`).
