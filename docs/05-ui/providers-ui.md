# Providers UI

## Main pages
- Provider list: `src/app/(dashboard)/dashboard/providers/page.js`.
- Provider detail: `src/app/(dashboard)/dashboard/providers/[id]/page.js`.

## Key UI components
- Connection rows, model rows, compatible model sections, add/edit modals.
- Shared cards in `src/app/(dashboard)/dashboard/providers/components/*`.

## Behavior link
- UI actions map to `/api/providers/**`, `/api/provider-nodes/**`, `/api/models/**`, and `/api/pricing`.
