# Stores and UI State

## Store index
- `src/store/index.js` exports Zustand stores.

## Store modules
- `providerStore`, `settingsStore`, `themeStore`, `notificationStore`, `userStore`, `headerSearchStore`.

## State role
- Stores coordinate dashboard form state, status updates, and UX-level preferences across route pages.
